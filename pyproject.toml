[tool.poetry]
package-mode = false

[tool.poetry.dependencies]
python = "^3.11"
setuptools = "65.6.3"
pre-commit = "2.19.0"
cookiecutter = "2.4.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"