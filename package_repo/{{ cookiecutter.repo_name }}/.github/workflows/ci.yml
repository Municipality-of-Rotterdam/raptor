{% raw %}
name: Continuous Integration

on:
  pull_request:

env:
  PYTHON_VERSION: {% endraw %}{{cookiecutter.python_version}}{% raw %}

jobs:
  pre-commit:
    uses: GemeenteRotterdam/raptor/.github/workflows/pre-commit.yml@main
    with:
      python_version: ${{ env.PYTHON_VERSION }}

  unittest:
    uses: GemeenteRotterdam/raptor/.github/workflows/unittest.yml@main
    with:
      python_version: ${{ env.PYTHON_VERSION }}
{% endraw %}